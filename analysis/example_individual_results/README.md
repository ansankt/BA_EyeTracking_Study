# Synthetischer Beispieldatensatz

Diese Dateien sind erfundene Testdaten fuer die Auswertung. Sie enthalten zwei Teilnehmende (`P01`, `P02`) mit jeweils einem Trial pro Modell und duerfen nicht mit realen Studiendaten vermischt werden.

Zum Testen der Gesamtauswertung:

```bash
python3 analysis/aggregate_study_results.py analysis/example_individual_results --out-dir /tmp/eye_study_example_results
```
